header:
  version: 10

local_conf_header:
  local image_install: |
    IMAGE_INSTALL:append:pn-core-image-minimal = " erlang erlang-modules elixir vernemq"
    IMAGE_ROOTFS_EXTRA_SPACE:pn-core-image-minimal = "1048576"
target:
    - core-image-minimal
